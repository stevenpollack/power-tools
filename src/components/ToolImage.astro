---
import { getEntry } from "astro:content";

interface Props {
  toolId: string;
}

const { toolId } = Astro.props;

const tool = await getEntry("tools", toolId);
const { name = "placeholder", image } = tool?.data ?? {};
---

<img src={image?.originalUrl} alt={name} />
